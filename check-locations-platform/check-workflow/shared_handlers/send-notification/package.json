{
  "name": "send-notification-handler",
  "version": "1.0.0",
  "description": "Lambda function to send notifications via SES.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.592.0"
  }
}
